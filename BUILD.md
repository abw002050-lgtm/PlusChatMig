# Build

The project targets Android API 35, min 24, Java/Kotlin 17.

A real Firebase project configuration is required for authentication and realtime services. Add a valid google-services.json for the package com.chatmig.modern before a production build.
